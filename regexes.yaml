user_agent_parsers:
  # iOS / OSX Applications
  - regex: '(iPod|iPod touch|iPhone|iPad);.*CPU.*OS[ +](\d+)_(\d+)(?:_(\d+)|).* like Gecko\) (?!Version\/[\d.]+)[A-Za-z]+\/[\d.]+'
    family_replacement: 'Mobile Safari UI/WKWebView'